name: ci
on:
  push:
    branches:
      - main
      - '[0-9]*.x'
  pull_request:

jobs:
  main:
    name: Tests
    runs-on: ubuntu-latest
    container: ghcr.io/spaceship-prompt/spaceship-prompt:latest
    steps:
      - name: ⬇️ Checkout Repo
        uses: actions/checkout@v6

      - name: 🖨 Print Zsh Version
        run: zsh --version

      - name: 🧪 Run Tests
        run: ./tests/spaceship-flox.test.zsh

  release:
    name: Release
    needs: main
    if: ${{ github.repository == 'pfridberg/spaceship-flox' && github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: ⬇️ Checkout Repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🚀 Release
        uses: cycjimmy/semantic-release-action@v6
        with:
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-license
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
